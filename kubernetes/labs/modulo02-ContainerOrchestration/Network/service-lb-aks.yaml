apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-lb-aks
  name: service-loadbalancer-aks
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-lb-aks
  type: LoadBalancer
status:
  loadBalancer: {}
