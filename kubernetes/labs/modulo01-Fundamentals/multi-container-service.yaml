apiVersion: v1
kind: Service
metadata:
  labels:
    app: webserver-pod
  name: webserver-pod
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: webserver-pod
  type: NodePort
