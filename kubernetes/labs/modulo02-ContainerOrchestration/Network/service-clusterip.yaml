apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-clusterip
  name: service-clusterip
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-clusterip
  type: ClusterIP
status:
  loadBalancer: {}
